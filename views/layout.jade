doctype 5
html
   head
      link(rel="shortcut icon", href="/favicon.ico")

      title kansascity2: what's on
         block title
            |?

      block styles
         != css("reset")
         != css("core")
         != css("event")
         != css("tipsy")
         
      block scripts
         script(src="https://ajax.googleapis.com/ajax/libs/jquery/1.7.2/jquery.min.js")
         != js("jquery.tipsy")
         != js("jquery.unspamify")
         != js("pages/layout")
         != js("pages/add_event")

   body(class=viewClass(locals))
      header
         h1 kansascity2: what's on

         .add
            nav
               ul
                  li.add_event
                     a(href="/add_event/") Add your event

      block body

      footer
         | for comments or if you see an error, contact 
         a.unspamify-me ascalonx at gmail dot com
         |  or @dustyburwell